name: Cloudbeds → Airtable occupancy sync

on:
  schedule:
    - cron: "7 * * * *"  # every hour at minute 7
  workflow_dispatch:

jobs:
  run-sync:
    runs-on: ubuntu-latest

    env:
      AIRTOP_API_KEY: "5ea5dc00641b3bd7.rpKfsOBOb5HDfdjf5KiNWAD4x11LVa88v6QxcYehdO"
      AIRTABLE_API_KEY:  "patPL1U7m1KTXz0Wh.0efce3ccf0de0ecfc66ce57f5521d83e2936d05bce9956db998d36647c8d910e"
      AIRTABLE_BASE_ID:  "appqstjfFNrdvbxIE"
      AIRTABLE_TABLE_ID: "tblOaAgG187IfNGwI"

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: |
          pip install httpx airtop requests

      - name: Run occupancy sync
        run: |
          python cloudbeds_occupancy_sync.py